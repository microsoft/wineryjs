language: node_js
compiler: default
node_js: 
  - '8'
  - '9'

before_install:
  - sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y
  - sudo apt-get update -q

install:
  - npm install

script:
  - npm test
